<div id='<%="escape_#{@escape.id}"%>'>
  <h1><%= @escape.title %></h1>

  <div id='photos' class='escape_flickr_photos'>
    <% @flickr_photos.each do |fp| %>
      <img alt="<%= fp.title %> src="<%=fp.url_med %>">
    <% end %>
  </div>
</div>
